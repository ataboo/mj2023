using Godot;

public enum MechAbility {
    Grab = 0,
    Kick,
    Plate,
    Sauce,
    Pepperoni,
    Cheese,
}

public enum MechLegState {
    Idle,
    Windup,
    Kick,
}

public enum CookedState {
    Raw,
    Cooking,
    Cooked,
    Burned
}